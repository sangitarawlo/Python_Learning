### 1. **Find the Maximum and Minimum Values in a Tuple**

#Problem Statement:
#Write a function `maxMin(t)` that takes a tuple of integers `t` and returns a new tuple containing the maximum and minimum values.


# Example 1
# Input: t = (3, 1, 4, 1, 5, 9)
# Output: (9, 1)

# Example 2
# Input: t = (7, -2, 5, 10, 0)
# Output: (10, -2)

# Example 3
# Input: t = ()
# Output: (None, None)  # Handling empty tuple




### 2. **Tuple to Dictionary**

#Problem Statement:
#Write a function `tupleToDict(t)` that takes a tuple of pairs (key, value) and converts it into a dictionary.


# Example 1
# Input: t = (('a', 1), ('b', 2), ('c', 3))
# Output: {'a': 1, 'b': 2, 'c': 3}

# Example 2
# Input: t = (('name', 'Alice'), ('age', 25))
# Output: {'name': 'Alice', 'age': 25}




### 3. **Check if a Tuple Contains Duplicates**

#Problem Statement:
#Write a function `hasDuplicates(t)` that takes a tuple `t` and returns `True` if there are any duplicate elements in the tuple, and `False` otherwise.


# Example 1
# Input: t = (1, 2, 3, 4)
# Output: False

# Example 2
# Input: t = (1, 2, 2, 3)
# Output: True

# Example 3
# Input: t = ()
# Output: False  # Handling empty tuple




### 4. **Tuple Unpacking**

#Problem Statement:
#Write a function `swap(a, b)` that takes two variables, `a` and `b`, and swaps their values using tuple unpacking. Return the swapped values as a tuple.


# Example 1
# Input: a = 5, b = 10
# Output: (10, 5)

# Example 2
# Input: a = 'hello', b = 'world'
# Output: ('world', 'hello')




### 5. **Count Elements in a Tuple**

#Problem Statement:
#Write a function `countElements(t)` that takes a tuple `t` and returns a dictionary with each element as the key and its count as the value.


# Example 1
# Input: t = (1, 2, 2, 3, 3, 3)
# Output: {1: 1, 2: 2, 3: 3}

# Example 2
# Input: t = ('apple', 'banana', 'apple')
# Output: {'apple': 2, 'banana': 1}

